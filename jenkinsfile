pipeline {
  agent any
  tools {
    maven 'M3'   // name must match the entry in Global Tool Configuration
    jdk 'jdk11'  // if defined in tools
  }
  stages {
    stage('Build') {
      steps {
        bat 'mvn -v'
        bat 'mvn -B -DskipTests package'
      }
    }
  }
}
