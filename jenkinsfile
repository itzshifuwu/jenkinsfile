pipeline {
  agent any
  environment {
    NODE_VERSION = '14'
    MY_VAR = 'shifa_value'
  }
  stages {
    stage('Build') {
      steps {
        echo "Using NODE_VERSION = ${env.NODE_VERSION}"
        echo "MY_VAR = ${env.MY_VAR}"
      }
    }
  }
}
